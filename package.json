{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "reset": "rm -rf node_modules && rm yarn.lock && yarn install",
    "newComponent": "npx nuxi add component UX",
    "newLayout": "npx nuxi add layout UX",
    "newComposable": "npx nuxi add composable UX",
    "newPage": "npx nuxi add page UX",
    "newModule": "npx nuxi init -t module my-module",
    "newLayer": "npx nuxi init -t layer my-layer"
  },
  "ntl": {
    "runner": "yarn",
    "descriptions": {
      "build": "Builds the project",
      "dev": "Starts the development server",
      "generate": "static site generation",
      "preview": "Starts the production server",
      "postinstall": "finishing touches after install",
      "reset": "completely resets the node_modules directory",
      "newComponent": "Create a new component",
      "newLayout": "Create a new layout",
      "newComposable": "Create a new composable",
      "newPage": "Create a new page",
      "newModule": "Create a new module",
      "newLayer": "Create a new layer"
    }
  },
  "devDependencies": {
    "@nuxt/content": "^2.11.0",
    "@nuxt/devtools": "latest",
    "@nuxt/eslint-config": "^0.2.0",
    "@nuxt/ui": "latest",
    "@nuxtjs/color-mode": "^3.3.2",
    "@nuxtjs/tailwindcss": "^6.11.2",
    "@vueuse/core": "^10.8.0",
    "@vueuse/integrations": "^10.8.0",
    "@vueuse/nuxt": "^10.7.2",
    "eslint": "^8.56.0",
    "nuxt": "latest"
  },
  "dependencies": {
    "@vuelidate/core": "^2.0.3",
    "@vuelidate/validators": "^2.0.4",
    "rehype-external-links": "^3.0.0",
    "vue-use-fixed-header": "^2.0.2",
    "vue3-markdown": "^1.1.9"
  },
  "resolutions": {
    "string-width": "4.2.3"
  }
}
